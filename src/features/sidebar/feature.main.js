// Phase 3: Will register sidebar IPC handlers
// Currently sidebar IPC is registered in src/electron/main.js
function registerSidebarMain() {
  // IPC handlers registered by existing main.js for now
}

module.exports = { registerSidebarMain };
